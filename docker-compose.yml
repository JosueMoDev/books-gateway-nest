services:
  rabbitmq:
    image: rabbitmq:3.9-management
    container_name: rabbitmq
    ports:
      - '5672:5672'
      - '15672:15672'
    environment:
      RABBITMQ_DEFAULT_USER: appbooksrbmq
      RABBITMQ_DEFAULT_PASS: str0ngPassw0rd
